# 后续迭代清单

以下尚未实现，不应在产品说明中标为已具备：

- 按 token 预算选择上下文、滚动摘要、按需检索更早聊天。
- 跨所有收藏页的稳定表情定位与自动目录同步。
- 对窗口权限错误更细分的恢复策略；仍需保持“未知发送结果不重发”。
- 模型配置的导入导出（默认不含 Key）、多设备迁移。
- 完整的历史记录分页与汇总报表。
- 生成取消时主动终止可取消的服务端请求。
- 将依赖源码结构的 AST 提取封装为更稳定的适配器接口。

优先级由真实故障和使用反馈决定，先补回归用例再修改。多联系人已实现独立存储、引擎和发送排队。每人独立模型/提示词、联系人删除管理仍待实现；公网访问尚不支持。
